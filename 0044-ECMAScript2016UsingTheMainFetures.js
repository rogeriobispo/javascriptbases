// array.include(value) //verify if the value exists on the array
// 4**5 operador de exponenciasação.
const arr = [1,2,3,4,5]
if(arr.includes(5))
  console.log('include')
else
  console.log('does not include')

console.log(Math.pow(4, 5)) //old way of exponenciasação
console.log(4**5) 
