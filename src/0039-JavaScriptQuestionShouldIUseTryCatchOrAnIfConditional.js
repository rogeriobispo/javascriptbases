// if statement or try catch
if (typeof mainFunction === 'function') 
  mainFunction()
else
  console.log('AlternativeCode')

// try catch is for exceptional circumstances, so use it to deal with
// those exceptions
// api conection database conection try catch
// don't use try catch for program logic
// user try catch for error handling
// try catch can be more expensive if exceptions occur.
// 
