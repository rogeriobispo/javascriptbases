// its is best to place script tags. the
// on the end of the file html last tag of the body
// there is other ways to

// with events the script tag cam remain in the head tags
document.addEventListener('DOMContentLoaded', function(){
  let h1 = document.querySelector('h1');
  console.log(h1)
})


//in the head its block the download of conent images assets and so on.
// tips to improve download
// Place the script tag at the end of the body tag to prevent blocking
// CombineJS file together
// mimify javascript files
